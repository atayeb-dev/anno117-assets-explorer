# RDA CONSOLE

You can download the RDAConsole release at [https://github.com/anno-mods/RdaConsole](https://github.com/anno-mods/RdaConsole) and uncompress it in this folder or change the `RDA_CONSOLE_EXEC` to `path/to/RdaConsole.exe` in `extract_rda.py` for `extract_rda.py` usage.